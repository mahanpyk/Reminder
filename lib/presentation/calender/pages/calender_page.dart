import 'package:flutter/material.dart';
import 'package:reminder/app/base/base_view.dart';

class CalenderPage extends BaseView {
  CalenderPage({Key? key}) : super(key: key);

  @override
  Widget body(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}
